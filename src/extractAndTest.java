
public class extractAndTest {
  public static int[][] extractSubArray(int[][] inArray, int rowMin, int rowMax, int colMin, int colMax){
    return null; // this is here so it will compile when before implementation and should be removed
  }
  public static boolean noneTrue(boolean[][] inArray) {
    return false; // this is here so it will compile when before implementation and should be removed
  }
}
